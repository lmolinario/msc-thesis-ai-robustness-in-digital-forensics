# Datasets

This directory contains the dataset artifacts used in the thesis pipeline, from raw acquisition to final split generation and forensic evaluation bundle construction.

The dataset workflow follows a human-in-the-loop methodology: raw heterogeneous sources are first technically validated and deduplicated, then manually reviewed and frozen into the final thesis dataset.

---

## Directory Structure

```text
datasets/
├── README.md
│
├── raw/
│   ├── downloaded_raw_archives/
│   ├── 01_kaggle_weapon/
│   ├── 02_deepfirearm/
│   ├── 03_google_scraped/
│   ├── 04_telegram_youtube/
│   └── 05_deepweb/
│
├── prepared/
│   ├── final_pool/
│   │   ├── images/
│   │   ├── metadata.csv
│   │   └── reports/
│   └── manifests/
│       └── review_manifest_full.csv
│
├── final/
│   ├── manifests/
│   │   ├── manual_selection_protocol_db.csv
│   │   ├── manual_selection_final_1500.csv
│   │   ├── manual_selection_adversarial_subset.csv
│   │   └── manual_selection_removed.csv
│   └── reports/
│       ├── manual_selection_summary.json
│       ├── manual_selection_log.csv
│       ├── manual_selection_state.json
│       └── backups/
│
├── splits/
│   ├── clean/
│   │   ├── fold_1/
│   │   ├── fold_2/
│   │   ├── fold_3/
│   │   ├── fold_4/
│   │   └── fold_5/
│   ├── ood/
│   │   └── ood_eval_set/
│   └── manifests/
│       ├── clean_folds_manifest.csv
│       ├── ood_eval_manifest.csv
│       └── split_generation_summary.json
│
├── forensic_evaluation_bundle/
│   ├── blind_tool_input/
│   ├── metadata/
│   └── structured_audit_view/
│
└── scripts/
    ├── utils/
    ├── acquisition/
    ├── prepared/
    ├── final/
    ├── splits/
    ├── attacks/
    └── bundle/
```

---

## Workflow

The dataset workflow is organized as follows:

```text
datasets/scripts/acquisition/
    ↓
datasets/raw/
    ↓
datasets/scripts/prepared/08_build_prepared_dataset.py
    ↓
datasets/prepared/final_pool/
    ↓
datasets/scripts/prepared/09_generate_review_manifest_full.py
    ↓
datasets/prepared/manifests/review_manifest_full.csv
    ↓
datasets/scripts/final/10_manual_selection_protocol_reviewer.py
    ↓
datasets/final/manifests/manual_selection_final_1500.csv
datasets/final/manifests/manual_selection_adversarial_subset.csv
    ↓
datasets/scripts/splits/11_generate_clean_and_ood_splits.py
    ↓
datasets/splits/clean/
datasets/splits/ood/
    ↓
datasets/scripts/attacks/13_generate_anti_forensic_attacks.py
datasets/scripts/attacks/14_generate_adversarial_attacks.py
    ↓
attacks/
    ↓
datasets/scripts/bundle/16_build_forensic_evaluation_bundle.py
    ↓
datasets/forensic_evaluation_bundle/
    ↓
Magnet AXIOM / Magnet.AI black-box processing
    ↓
evaluation/scripts/19_normalize_forensic_ai_tool_predictions.py
    ↓
evaluation/forensic_tools/
results/metrics/forensic_tools_metrics.csv
```

---

## Raw Data

Raw acquired data are stored in:

```text
datasets/raw/
```

This directory contains the original source groups used to build the prepared dataset.

Important distinction:

```text
datasets/raw/                  = raw acquired data
datasets/scripts/acquisition/  = acquisition scripts
```

The `datasets/raw/` directory must not be renamed to `acquisition`.

---

## Prepared Dataset

The prepared dataset is generated by:

```text
datasets/scripts/prepared/08_build_prepared_dataset.py
```

It performs:

- image discovery;
- technical validation;
- minimum size filtering;
- SHA256 computation;
- exact duplicate removal;
- copy into the prepared image pool;
- technical metadata generation;
- invalid-image and duplicate-discard reports.

Main output:

```text
datasets/prepared/final_pool/metadata.csv
```

The prepared dataset is a technical pool. It does not contain the final semantic class selection.

---

## Review Manifest

The full review manifest is generated by:

```text
datasets/scripts/prepared/09_generate_review_manifest_full.py
```

Main output:

```text
datasets/prepared/manifests/review_manifest_full.csv
```

This file bridges the technical preparation stage and the manual semantic review stage.

---

## Final Frozen Dataset

The official frozen dataset is:

```text
datasets/final/manifests/manual_selection_final_1500.csv
```

Final class distribution:

| Class | Count |
|---|---:|
| weapon | 500 |
| non_weapon | 500 |
| ood | 500 |

The previous `33_final_frozen_dataset.csv` naming convention is not used.

---

## Adversarial / Anti-Forensic Subset

The official binary subset for clean fold generation and attack generation is:

```text
datasets/final/manifests/manual_selection_adversarial_subset.csv
```

Class distribution:

| Class | Count |
|---|---:|
| weapon | 500 |
| non_weapon | 500 |

OOD images are excluded from this subset and evaluated separately.

---

## Split Generation

Clean binary folds and the OOD evaluation set are generated by:

```text
datasets/scripts/splits/11_generate_clean_and_ood_splits.py
```

Clean folds:

```text
datasets/splits/clean/fold_1/
datasets/splits/clean/fold_2/
datasets/splits/clean/fold_3/
datasets/splits/clean/fold_4/
datasets/splits/clean/fold_5/
```

Each fold contains:

| Class | Count |
|---|---:|
| weapon | 100 |
| non_weapon | 100 |

OOD evaluation set:

```text
datasets/splits/ood/ood_eval_set/ood/
```

OOD images are not split into folds.

---

## Split Manifests

Generated manifests:

```text
datasets/splits/manifests/clean_folds_manifest.csv
datasets/splits/manifests/ood_eval_manifest.csv
datasets/splits/manifests/split_generation_summary.json
```

These files provide traceability for:

- `image_id`;
- class label;
- fold assignment;
- source dataset;
- prepared image path;
- split image path;
- SHA256;
- MD5;
- file size;
- sample type.

---

## Attack Outputs

Attack generation is performed after split generation and proxy model training.

Anti-forensic transformations:

```text
attacks/anti_forensic/jpeg_recompression/
attacks/anti_forensic/resample_resize/
attacks/anti_forensic/gaussian_blur/
attacks/anti_forensic/histogram_modification/
attacks/anti_forensic/contrast_stretching/
```

Adversarial attacks:

```text
attacks/adversarial/fgsm/
attacks/adversarial/superdeepfool/
attacks/adversarial/sigma_zero/
attacks/adversarial/one_pixel/
attacks/adversarial/color_shift/
```

Attack outputs are not part of `datasets/`, but they are linked to dataset samples through manifests, hashes, fold identifiers, labels, and original image identifiers.

---

## Forensic Evaluation Bundle

The forensic evaluation bundle is generated by:

```text
datasets/scripts/bundle/16_build_forensic_evaluation_bundle.py
```

Output directory:

```text
datasets/forensic_evaluation_bundle/
```

Methodological purpose:

- provide blind file inputs for forensic AI tools;
- avoid leaking class, attack, or provenance information through filenames shown to tools;
- preserve internal traceability through metadata and hashes;
- support post-export normalization and audit of commercial forensic-tool outputs.

Logical areas:

```text
datasets/forensic_evaluation_bundle/blind_tool_input/
datasets/forensic_evaluation_bundle/metadata/
datasets/forensic_evaluation_bundle/structured_audit_view/
```

The bundle includes clean images, adversarial outputs, anti-forensic outputs, and OOD samples selected for black-box forensic-tool evaluation.

---

## Commercial Forensic-Tool Status

The forensic evaluation bundle has been processed with:

```text
Magnet AXIOM / Magnet.AI
```

The Magnet AXIOM / Magnet.AI export has been normalized through:

```text
evaluation/scripts/19_normalize_forensic_ai_tool_predictions.py
```

Consolidated normalized outputs:

```text
evaluation/forensic_tools/normalized_predictions.csv
evaluation/forensic_tools/magnet_axiom_normalized_predictions.csv
evaluation/forensic_tools/normalization_summary.json
results/metrics/forensic_tools_metrics.csv
results/metrics/magnet_axiom_metrics.csv
```

Additional commercial tools remain planned extensions unless comparable exports become available:

```text
X-Ways Forensics / Excire Photo AI
Cellebrite UFED
Oxygen Forensic Detective
```

---

## Traceability

Dataset traceability is based on:

- stable `image_id` values;
- SHA256 hashing;
- MD5 hashing for forensic tool compatibility;
- source metadata;
- relative paths;
- stage-specific manifests;
- manual review logs;
- split generation summaries;
- attack manifests;
- forensic bundle metadata;
- normalized forensic-tool outputs.

SHA256 is the primary integrity hash for the thesis pipeline.

---

## Current Status

Completed stages:

- dataset acquisition structure;
- prepared dataset construction;
- full review manifest generation;
- manual selection and final frozen dataset construction;
- clean fold generation;
- OOD evaluation set generation;
- anti-forensic transformation generation;
- adversarial attack generation;
- forensic evaluation bundle construction and validation;
- Magnet AXIOM / Magnet.AI processing;
- Magnet AXIOM / Magnet.AI export normalization;
- commercial-tool metrics generation for Magnet AXIOM / Magnet.AI.

Current stage:

```text
thesis reporting, XAI case-study finalization, and optional extension to additional commercial forensic tools
```

Next operational step:

```text
finalize Chapter 5 reporting and keep X-Ways/Excire, Cellebrite UFED and Oxygen as pending extensions unless normalized exports become available
```
