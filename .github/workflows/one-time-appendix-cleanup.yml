name: One-time Appendix Cleanup

on:
  push:
    branches: [ main ]
    paths:
      - '.github/workflows/one-time-appendix-cleanup.yml'

permissions:
  contents: write

jobs:
  cleanup:
    name: Remove obsolete progress references
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Remove obsolete progress blocks
        shell: python
        run: |
          from pathlib import Path

          targets = [
              Path('docs/LatexThesis/sections/07_appendix.tex'),
              Path('docs/LatexThesis_ITA/sections/07_appendix.tex'),
          ]
          block = (
              '  progress/\n'
              '    milestones/\n'
              '    logs/\n'
              '    notes/\n'
          )

          for path in targets:
              text = path.read_text(encoding='utf-8')
              occurrences = text.count(block)
              if occurrences != 1:
                  raise SystemExit(
                      f'Expected exactly one obsolete progress block in {path}; found {occurrences}'
                  )
              path.write_text(text.replace(block, '', 1), encoding='utf-8')
              if 'progress/' in path.read_text(encoding='utf-8'):
                  raise SystemExit(f'Residual progress reference found in {path}')

          Path('.github/workflows/one-time-appendix-cleanup.yml').unlink()
          print('Removed obsolete progress blocks from both appendices.')

      - name: Run repository audit checks
        shell: bash
        run: |
          python - <<'PY'
          import json
          import py_compile
          from pathlib import Path

          required = [
              'README.md',
              'THESIS_ARTIFACT.md',
              'REPOSITORY_MAP.md',
              'ARTIFACT_EVALUATION.md',
              'DATA_DICTIONARY.md',
              'ENVIRONMENT.md',
              'RELEASE_CHECKLIST.md',
              'CHANGELOG.md',
              'CITATION.cff',
              'DATA_ACCESS.md',
              'SECURITY.md',
              'REPRODUCIBILITY.md',
              'ACADEMIC_REPOSITORY_AUDIT.md',
              'requirements.txt',
              'LICENSE',
              'docs/LatexThesis/main.tex',
              'evaluation/scripts/19_normalize_forensic_ai_tool_predictions.py',
              'results/scripts/20_generate_experimental_reporting_assets.py',
              'results/scripts/21_generate_embedded_metadata_sensitivity_check.py',
          ]
          missing = [path for path in required if not Path(path).exists()]
          if missing:
              raise SystemExit(f'Missing required files: {missing}')

          selected_json = [
              'datasets/splits/manifests/split_generation_summary.json',
              'datasets/forensic_evaluation_bundle/metadata/bundle_summary.json',
              'evaluation/forensic_tools/normalization_summary.json',
              'models/model_registry.json',
          ]
          for item in selected_json:
              path = Path(item)
              if path.exists():
                  json.loads(path.read_text(encoding='utf-8'))

          scripts = [
              'datasets/scripts/acquisition/00_download_raw_datasets_bundle.py',
              'datasets/scripts/acquisition/01_download_kaggle.py',
              'datasets/scripts/acquisition/02_download_github.py',
              'datasets/scripts/acquisition/03_build_subset_deepfirearm.py',
              'datasets/scripts/acquisition/04_scrape_google.py',
              'datasets/scripts/acquisition/05_scrape_telegram.py',
              'datasets/scripts/acquisition/06_scrape_youtube.py',
              'datasets/scripts/acquisition/07_scrape_deepweb.py',
              'datasets/scripts/prepared/08_build_prepared_dataset.py',
              'datasets/scripts/prepared/09_generate_review_manifest_full.py',
              'datasets/scripts/final/10_manual_selection_protocol_reviewer.py',
              'datasets/scripts/splits/11_generate_clean_and_ood_splits.py',
              'models/scripts/12_train_proxy_models.py',
              'datasets/scripts/attacks/13_generate_anti_forensic_attacks.py',
              'datasets/scripts/attacks/14_generate_adversarial_attacks.py',
              'evaluation/scripts/15_evaluate_proxy_models.py',
              'datasets/scripts/bundle/16_build_forensic_evaluation_bundle.py',
              'explainability/scripts/17_generate_integrated_gradients_case_studies.py',
              'explainability/scripts/18_xai_interactive_launcher.py',
              'evaluation/scripts/19_normalize_forensic_ai_tool_predictions.py',
              'results/scripts/20_generate_experimental_reporting_assets.py',
              'results/scripts/21_generate_embedded_metadata_sensitivity_check.py',
          ]
          for item in scripts:
              path = Path(item)
              if not path.exists():
                  raise SystemExit(f'Missing official script: {path}')
              py_compile.compile(str(path), doraise=True)

          patterns = [
              '/run/media/lello',
              'X-Ways',
              'Oxygen Forensic Detective',
              'Autopsy',
          ]
          checked_paths = [
              Path('README.md'),
              Path('THESIS_ARTIFACT.md'),
              Path('REPOSITORY_MAP.md'),
              Path('ARTIFACT_EVALUATION.md'),
              Path('DATA_DICTIONARY.md'),
              Path('ENVIRONMENT.md'),
              Path('RELEASE_CHECKLIST.md'),
              Path('CHANGELOG.md'),
              Path('REPRODUCIBILITY.md'),
              Path('DATA_ACCESS.md'),
              Path('SECURITY.md'),
              Path('docs/README.md'),
              Path('datasets/README.md'),
              Path('attacks/README.md'),
              Path('evaluation/README.md'),
              Path('forensic_tools/README.md'),
              Path('results/README.md'),
              Path('explainability/README.md'),
          ]
          for path in checked_paths:
              if not path.exists():
                  continue
              text = path.read_text(encoding='utf-8', errors='ignore')
              for pattern in patterns:
                  if pattern in text:
                      raise SystemExit(f'Forbidden or stale pattern {pattern!r} found in {path}')

          for path in [
              Path('docs/LatexThesis/sections/07_appendix.tex'),
              Path('docs/LatexThesis_ITA/sections/07_appendix.tex'),
          ]:
              if 'progress/' in path.read_text(encoding='utf-8', errors='ignore'):
                  raise SystemExit(f'Residual progress reference found in {path}')

          print('Repository audit checks passed after appendix cleanup.')
          PY

      - name: Commit cleanup
        shell: bash
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git commit -m 'docs: remove obsolete progress references from appendices'
          git push origin HEAD:main
