\chapter{Implementation and Experiments}	\label{chap:Implementation}

\begin{comment}
Qui dare i dettagli sulla metodologia descritta in precedenza.

Presentare i risultati.

Spiegare e discutere i risultati, quindi dare un significato ai numeri ottenuti.

DATASET:
- kaggle
- scraping google
- deep web scraping
- social network
Dataset diversi in modo essere più forte lato tecnico dimostrativo. Circa 1000 immagini.

TOOL: XWays (si trova), Magnet Axiom (ce lo danno), UFED (dovremmo averlo), Oxygen (?)

PERTURBAZIONI IMMAGINI: 
1. perturbazioni standard:
- le più note e utilizzate
- le più recenti
2. perturbazioni applicate alla forense:
- anti-forense
- \textit{per renderle più "sicure"} (da verificare)

Preso il dataset, presi gli algoritmi. Si cercano gli script per modificare le immagini e quindi creare il dataset delle immagini adversarial.

Una volta creato il dataset si carica su pennina USB (filesystem pulito senza immagini OS e FS quindi nessuna influenza).

Si acquisisce la pennina. Si carica l'immagine della pennina nei tool. Si estraggono i report/risultati. Si ripete la procedura con le immagini originali senza perturbazione per confrontare i risultati.

Si descrivono i risultati sia numericamente che qualitativamente spiegando il significato. Si fa correlazione tra risultati e tecnica adversariale usata. 


Uno step da fare dopo tutto. Se io aggiungo xAI. Noi non abbiamo algoritmo dei tool quindi test adversarial black box. 
C'è un modo per creare un modulo aggiuntivo al tool che utilizzi xAI e verificare i risultati. 

Oppure si può prendere un algoritmo molto ben performante allo stato dell'arte sul riconoscimento di immagini, testare con quel dataset nostro perturbato e a questo algoritmo whitebox applicare xAI e se performa meglio, suggerire poi di integrare tecniche simili nei tool forensi.


\end{comment}