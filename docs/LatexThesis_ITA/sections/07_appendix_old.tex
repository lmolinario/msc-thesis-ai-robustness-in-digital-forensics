\chapter{Repository and Reproducibility Artifacts}
\label{chap:appendix}

Questa appendice raccoglie gli elementi tecnici necessari a documentare la riproducibilità del lavoro sperimentale. Il codice sorgente completo non viene riportato integralmente nel documento, ma è mantenuto nella repository associata alla tesi. Le sezioni seguenti descrivono la struttura logica del progetto, gli artefatti sperimentali principali, i manifest ufficiali, i meccanismi di tracciabilità, i comandi essenziali di esecuzione e alcuni esempi rappresentativi di output. Tale scelta consente di preservare la leggibilità della tesi, evitando la duplicazione integrale dei file sorgente e mantenendo il codice nella sede più adatta alla sua consultazione, verifica e aggiornamento.

\section{Struttura della repository}
\label{app:repository-structure}

La repository è organizzata in modo modulare, separando i dati grezzi, gli artefatti preparati, i manifest finali, gli split sperimentali, gli script della pipeline, gli output di valutazione e la documentazione della tesi. Tale organizzazione consente di distinguere chiaramente tra dati di input, fasi di trasformazione, artefatti congelati e risultati sperimentali.

\begin{lstlisting}[caption={Struttura semplificata della repository sperimentale.}, label={lst:repository-structure}]
msc-thesis-ai-robustness-in-digital-forensics/
  datasets/
    raw/
    prepared/
    final/
    splits/
    forensic_evaluation_bundle/
  datasets/scripts/
    prepared/
    final/
    splits/
    bundle/
  attacks/
    adversarial/
    anti_forensic/
  evaluation/
    clean/
    adversarial/
    anti_forensic/
    forensic_tools/
  explainability/
    outputs/
    manifests/
  results/
    metrics/
    tables/
    plots/
    figures/
    reports/
  docs/
    LatexThesis_ITA/
\end{lstlisting}


\begin{figure}[H]
    \centering
    \includegraphics[width=0.28\textwidth]{images/msc_thesis_repo_qr.png}
    \caption{QR code per l'accesso diretto alla repository GitHub della tesi.}
    \label{fig:repo-qrcode}
\end{figure}


\section{Artefatti sperimentali principali}
\label{app:experimental-artifacts}

La Tabella~\ref{tab:experimental-artifacts} riassume i principali artefatti sperimentali utilizzati nella pipeline. Tali file costituiscono il riferimento operativo per la ricostruzione del dataset, la generazione degli split, la valutazione dei modelli e la predisposizione del bundle destinato agli strumenti forensi.

\begin{table}[H]
\centering
\caption{Principali artefatti sperimentali della pipeline.}
\label{tab:experimental-artifacts}
\begin{tabularx}{\textwidth}{@{}lX@{}}
\toprule
\textbf{Artefatto} & \textbf{Ruolo nella pipeline} \\
\midrule
\texttt{manual\_selection\_final\_1500.csv} & Manifest ufficiale del dataset frozen, composto da 1500 immagini bilanciate nelle classi \texttt{weapon}, \texttt{non\_weapon} e \texttt{OOD}. \\
\texttt{manual\_selection\_adversarial\_subset.csv} & Manifest del subset binario utilizzato per gli esperimenti adversariali e anti-forensi, composto dalle sole classi \texttt{weapon} e \texttt{non\_weapon}. \\
\texttt{manual\_selection\_protocol\_db.csv} & Database operativo del protocollo di selezione manuale \textit{human-in-the-loop}. \\
\texttt{manual\_selection\_removed.csv} & Manifest dei campioni esclusi durante la selezione finale. \\
\texttt{clean\_folds\_manifest.csv} & Manifest degli split puliti utilizzati per la valutazione baseline dei modelli. \\
\texttt{ood\_eval\_manifest.csv} & Manifest del set \texttt{OOD} utilizzato per la valutazione fuori distribuzione. \\
\texttt{bundle\_manifest.csv} & Manifest globale del bundle di valutazione destinato agli strumenti forensi. \\
\texttt{bundle\_hashes\_sha256.csv} & File contenente gli hash SHA256 degli artefatti inclusi nel bundle forense. \\
\bottomrule
\end{tabularx}
\end{table}

\section{Ambiente software e librerie utilizzate}
\label{app:software-environment}

La pipeline sperimentale è stata implementata prevalentemente in Python, utilizzando librerie per la gestione dei dati, l'elaborazione delle immagini, l'esecuzione dei modelli e la produzione degli output sperimentali. La Tabella~\ref{tab:software-environment} riassume i principali strumenti software impiegati.

\begin{table}[H]
\centering
\caption{Principali librerie e strumenti software utilizzati nella pipeline sperimentale.}
\label{tab:software-environment}
\begin{tabularx}{\textwidth}{@{}lX@{}}
\toprule
\textbf{Libreria / Strumento} & \textbf{Funzione principale} \\
\midrule
\texttt{Python} & Linguaggio principale per la costruzione della pipeline sperimentale. \\
\texttt{pandas} & Gestione dei manifest, dei metadati e dei risultati sperimentali. \\
\texttt{NumPy} & Gestione numerica di array, immagini e matrici di perturbazione. \\
\texttt{Pillow} & Validazione, apertura e trasformazione delle immagini. \\
\texttt{OpenCV} & Elaborazione di immagini e applicazione di trasformazioni anti-forensi. \\
\texttt{PyTorch} & Esecuzione dei modelli neurali e generazione di alcune perturbazioni adversariali. \\
\texttt{scikit-learn} & Addestramento e valutazione della baseline \gls{svm} e calcolo delle metriche. \\
\texttt{matplotlib} & Produzione di grafici, curve e figure sperimentali. \\
\texttt{CLIP} & Classificazione zero-shot e confronto semantico immagine-testo. \\
\texttt{BLIP} & Captioning e analisi multimodale delle immagini. \\
\bottomrule
\end{tabularx}
\end{table}

\section{Manifest, hash e tracciabilità}
\label{app:manifests-hashes}

La tracciabilità degli artefatti sperimentali è garantita attraverso l'uso sistematico di manifest e hash crittografici. Ogni immagine viene associata a metadati tecnici e semantici che consentono di collegare il campione alla sorgente originaria, alla classe assegnata, allo split sperimentale, all'eventuale versione perturbata e agli output prodotti dai modelli o dagli strumenti forensi.

Il calcolo degli hash SHA256 costituisce un elemento centrale della pipeline, poiché consente di verificare l'identità dei file, individuare duplicati e mantenere un collegamento stabile tra immagini pulite, immagini trasformate e risultati sperimentali. Nel bundle di valutazione forense, gli hash rappresentano inoltre il principale criterio di raccordo tra gli artefatti importati nei tool professionali e gli output esportati dagli stessi strumenti.

\section{Comandi principali per la riproducibilità}
\label{app:reproducibility-commands}

La pipeline sperimentale è organizzata in script separati, ciascuno dedicato a una fase specifica del workflow. Il Listato~\ref{lst:dataset-pipeline-commands} riporta la sequenza logica principale per ricostruire gli artefatti del dataset e predisporre il bundle di valutazione forense.

\begin{lstlisting}[caption={Sequenza principale di esecuzione della pipeline dataset.}, label={lst:dataset-pipeline-commands}]
python datasets/scripts/prepared/08_build_prepared_dataset.py
python datasets/scripts/prepared/09_generate_review_manifest_full.py
python datasets/scripts/final/10_manual_selection_protocol_reviewer.py
python datasets/scripts/splits/11_generate_clean_and_ood_splits.py
python datasets/scripts/bundle/12_build_forensic_evaluation_bundle.py
\end{lstlisting}

\section{Esempi di log e output}
\label{app:logs-outputs}

Durante l'esecuzione della pipeline, gli script producono log e report intermedi utili a verificare la correttezza delle operazioni effettuate. Il Listato~\ref{lst:dataset-preparation-log} mostra un esempio schematico di output relativo alla fase di preparazione tecnica del dataset.

\begin{lstlisting}[caption={Esempio schematico di output della fase di preparazione del dataset.}, label={lst:dataset-preparation-log}]
[INFO] Scanning raw dataset sources...
[INFO] Valid images detected: ...
[INFO] Invalid images discarded: ...
[INFO] SHA256 hashes computed: ...
[INFO] Global duplicates removed: ...
[INFO] Prepared image pool written to datasets/prepared/final_pool/images/
[INFO] Metadata written to datasets/prepared/final_pool/metadata.csv
\end{lstlisting}

\section{Repository e disponibilità del codice}
\label{app:repository-availability}

Il codice completo utilizzato per la costruzione del dataset, la generazione degli split, la produzione delle perturbazioni, l'esecuzione delle valutazioni e la raccolta dei risultati è mantenuto nella repository associata alla tesi. La repository contiene inoltre i manifest principali, gli script della pipeline e la documentazione operativa necessaria alla riproduzione del workflow sperimentale.

\begin{itemize}
    \item \textbf{Repository GitHub:} \codepath{https://github.com/lmolinario/msc-thesis-ai-robustness-in-digital-forensics}
    \item \textbf{Manifest ufficiale del dataset frozen:} \codepath{datasets/final/manifests/manual\_selection\_final\_1500.csv}
    \item \textbf{Manifest del subset adversarial:} \codepath{datasets/final/manifests/manual\_selection\_adversarial\_subset.csv}
    \item \textbf{Licenza:} da definire nella repository finale.
\end{itemize}
