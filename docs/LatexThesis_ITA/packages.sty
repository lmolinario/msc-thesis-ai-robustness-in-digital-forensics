\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{packages}[2025/03/03 Packages]

% ------ Contents -------
% Appendices
% Acronyms
% Bibliography & References
% Flow charts
% Front matter
% General
% Image
% Language
% Main matter
% Mathematics
% Page setup
% Tables
% Chapter title format
% Code listings
% Header and footer
% -----------------------

% --- Encoding ---
\usepackage[utf8]{inputenc}

% --- Appendices ---
\usepackage{appendix}
\newcommand{\addappendix}{
    \newpage
    \appendix
    \section*{Appendix}
    \addcontentsline{toc}{section}{Appendix}
    \renewcommand{\thesubsection}{\Alph{subsection}}
}

% --- Acronyms ---
\usepackage[acronym,toc,nonumberlist,nopostdot]{glossaries}
\makeglossaries

% --- Bibliography & References ---
\usepackage[
backend=biber,
style=numeric,
sorting=none,
sortcites=true
]{biblatex}
\addbibresource{tesi.bib}
\usepackage{csquotes}
\usepackage[nottoc]{tocbibind}
\usepackage{caption}
\usepackage{subcaption}
\newcommand{\source}[1]{\vspace{-4pt} \caption*{\hfill \footnotesize{Source: {#1}}}}

% --- General ---
\usepackage{import}
\usepackage{afterpage}
\usepackage[bottom]{footmisc}
\usepackage{comment}
\usepackage{verbatim}
\usepackage{pdfpages}
\usepackage{xcolor}
\usepackage{colortbl}
\usepackage{fontawesome5}

% --- Image ---

\usepackage{graphicx}
\usepackage{wrapfig}
\usepackage{float}
\newcommand{\HRule}{\rule{\linewidth}{0.5mm}}

% --- Flow Charts and Diagrams ---
\usepackage{tikz}
\usetikzlibrary{shapes,arrows,arrows.meta,positioning}
\tikzstyle{decision} = [diamond, draw, fill=blue!20,
    text width=4.5em, text badly centered, node distance=3cm, inner sep=0pt]
\tikzstyle{block} = [rectangle, draw, fill=blue!20,
    text width=5em, text centered, rounded corners, minimum height=4em]
\tikzstyle{line} = [draw, -latex']
\tikzstyle{cloud} = [draw, ellipse, fill=red!20, node distance=3cm,
    minimum height=2em]
\usepackage{smartdiagram}
\usesmartdiagramlibrary{additions}

% --- Front matter ---
\newcommand{\frontmatter}{
    \pagenumbering{roman}
}

% --- Language ---
\usepackage{siunitx}
\usepackage[british]{babel}
\addto\extrasbritish{
    \renewcommand*\contentsname{Contents}
    \renewcommand{\listfigurename}{List of Figures}
    \renewcommand{\listtablename}{List of Tables}
    \def\equationautorefname{Equation}
    \def\figureautorefname{Figure}
    \def\tableautorefname{Table}
    \def\sectionautorefname{Section}
    \def\subsectionautorefname{\sectionautorefname}
    \def\subsubsectionautorefname{\sectionautorefname}
}

% --- Main matter ---
\newcommand{\mainmatter}{
    \newpage
    \pagenumbering{arabic}
}

% --- Mathematics ---
\usepackage{bm}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{amsthm}
\usepackage{textcomp}
\usepackage{gensymb}

% --- Page setup ---
\usepackage[a4paper,top=3cm,bottom=3cm,left=3.5cm,right=3cm]{geometry}
\usepackage{parskip}

% --- Tables ---
\usepackage{longtable}
\usepackage{tabularx}
\usepackage{booktabs}

% --- Chapter title format ---
\usepackage{titlesec}

% --- Code listings ---
\usepackage{listings}
\lstset{
    language=Python,
    basicstyle=\ttfamily\footnotesize,
    numbers=left,
    numberstyle=\tiny,
    stepnumber=1,
    numbersep=5pt,
    backgroundcolor=\color{white},
    showspaces=false,
    showstringspaces=false,
    showtabs=false,
    frame=single,
    tabsize=2,
    captionpos=b,
    breaklines=true,
    breakatwhitespace=false,
    keywordstyle=\color{blue}\bfseries,
    commentstyle=\color{green!50!black}\itshape,
    stringstyle=\color{red!70!black}
}

% --- Hyperlinks ---
\usepackage{xurl}
\usepackage{hyperref}
\hypersetup{
    colorlinks,
    citecolor=black,
    filecolor=black,
    linkcolor=black,
    urlcolor=black
}

% Breakable monospaced paths for scripts, manifests and repository artifacts.
\newcommand{\codepath}[1]{%
    \begingroup
    \urlstyle{tt}%
    \nolinkurl{#1}%
    \endgroup
}

% --- Header and footer ---
\usepackage{fancyhdr}
\usepackage{emptypage}

% Avoid fancyhdr headheight warnings.
\setlength{\headheight}{15pt}

% Front matter:
% no header, bottom rule, centered page number.
\fancypagestyle{frontmatterstyle}{
    \fancyhf{}
    \renewcommand{\headrulewidth}{0pt}
    \renewcommand{\footrulewidth}{0.4pt}
    \fancyfoot[C]{\thepage}
}

% Main matter:
% chapter title in the header, top rule,
% bottom rule and centered page number.
\fancypagestyle{mainmatterstyle}{
    \fancyhf{}
    \renewcommand{\headrulewidth}{0.4pt}
    \renewcommand{\footrulewidth}{0.4pt}
    \fancyhead[C]{\small\nouppercase{\leftmark}}
    \fancyfoot[C]{\thepage}
}

% Chapter-opening pages and other "plain" pages:
% no header title, only bottom rule and page number.
\fancypagestyle{plain}{
    \fancyhf{}
    \renewcommand{\headrulewidth}{0pt}
    \renewcommand{\footrulewidth}{0.4pt}
    \fancyfoot[C]{\thepage}
}